// chrome.browserAction.onClicked.addListener(function(activeTab){

//   chrome.tabs.query({
//       active: true,
//       currentWindow: true
//   }, function(tabs) {
//       var tab = tabs[0];
//       var url = tab.url;

//       const siteCheckerUrl = "http://localhost:8080/report/" + url;
//       chrome.tabs.create({ url: siteCheckerUrl });
//       console.log(url);
//       console.log(`aaaaaa`);
      
      
//   });
// });

// App.init();
